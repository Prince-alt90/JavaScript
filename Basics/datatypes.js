"use strict";  // tret all JS code as newer version

// alert(3+3)  // it will execute in browser but  not in node

console.log("Prince");

let name="Hello"
let age=15
let isLogged=true

/* Primitve Datatype
 number (range is 2 to power 53)
   bigint
   String
   boolean
   null   => standalone value (it is a special type)
   undefined =>
    Symbol  =>unique
*/

//object

console.log(typeof "Prince"); //String
console.log(typeof age);  //number
console.log(typeof null);  //Null is  object (somepeople say it is failure of language)
console.log(typeof undefined);  // undefined  (undefined is a type in ownself)



