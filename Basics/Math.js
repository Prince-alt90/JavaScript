const num2=15.214

// console.log(Math.floor(num2));
// console.log(Math.ceil(num2));

// console.log(Math.random());    //it will generate random numbers  between 0 and 1

const min=1
const max=6
console.log(Math.floor(Math.random()*(max-min+1)+min)); //it will generate random numbers between 1 and 6